import 'package:flutter/material.dart';

class WrapRemovalScreen extends StatelessWidget {
  const WrapRemovalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
